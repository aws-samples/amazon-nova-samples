display_name: "Nova Lite 2.0 RFT Evaluation on P5"
version: "1.0.0"
instance_types: ['ml.p5.48xlarge']

run:
  name: nova-lite-v2-rft-eval-job
  model_type: amazon.nova-2-lite-v1:0:256k
  model_name_or_path: nova-lite-2/prod # or s3://escrow_bucket/model_location
  replicas: 1 # modifiable for Sagemaker Training job, unmodifiable for Sagemaker Hyperpod job
  data_s3_path: "" # Leave empty for Sagemaker Training job, required for Sagemaker Hyperpod job
  output_s3_path: "" # (Required) Output artifact path, Sagemaker Hyperpod job-specific configuration - not compatible with Sagemaker Training jobs

evaluation:
  task: rft_eval
  strategy: rft_eval
  metric: "all" 

# Optional Inference configs
inference:
  max_new_tokens: 8196
  top_k: -1
  top_p: 1.0
  temperature: 0
  top_logprobs: 0 # Change to a value between (0,20] to enable
  reasoning_effort: null  # Enable reasoning, options are low, medium, high

# =============================================================================
# Bring Your Own Reinforcement Learning Environment
# =============================================================================
rl_env:
  mode: single_turn  # Options: "single_turn", "multi_turn"

  # Single Turn Configuration
  single_turn:
    # For SMTJ/SMHP short processing environment, use this setting
    lambda_arn: arn:aws:lambda:us-west-2:<account_id>:function:<reward-function-name>
